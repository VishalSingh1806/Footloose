import ChatList from './ChatList';

/**
 * MessagesPage
 *
 * Main messages page accessible from bottom nav
 * Wrapped by AppShell with TopBar and BottomNav
 */
function MessagesPage() {
  return <ChatList />;
}

export default MessagesPage;
